# Copy to .env / .dev.vars (per app) and fill. NEVER commit real secrets.
# Platform provider keys are managed via Cloudflare Secrets Store in production
# (see IMPLEMENTATION_PLAN.md §9). These are for local development only.

# --- API (apps/api/.dev.vars) ---
# AI_GATEWAY_ACCOUNT_ID=
# AI_PROVIDER_API_KEY=
# TRANSCRIPTION_PROVIDER_API_KEY=
# ENVELOPE_MASTER_KEY=

# --- Landing (apps/landing) ---
VITE_REQUEST_ACCESS_EMAIL=hi@speqify.app
